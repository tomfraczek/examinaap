import { NavigationProp } from '@react-navigation/native';
import { FIREBASE_AUTH as auth } from '../../firebaseConfig';
import { View, Button, ButtonText, Text } from '@gluestack-ui/themed';

export const Profile = () => {
  return (
    <View>
      <Text>Profile</Text>
    </View>
  );
};
